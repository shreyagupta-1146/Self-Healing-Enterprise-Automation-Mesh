{
  "_comment": "backend: 'sentinel' (SSHA, default, zero-cloud) | 'telegram' (legacy) | 'console' (tests only)",
  "backend": "sentinel",
  "sentinel": {
    "_comment": "SentinelNotifier — self-hosted SSHA. No config needed; alerts go to server console + logs/alert_queue.jsonl and admin dashboard."
  },
  "telegram": {
    "_comment": "Legacy fallback. Requires TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID env vars.",
    "bot_token_env": "TELEGRAM_BOT_TOKEN",
    "chat_id_env": "TELEGRAM_CHAT_ID"
  }
}
